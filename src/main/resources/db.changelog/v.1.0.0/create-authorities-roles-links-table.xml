<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <changeSet id="3" author="wladponomar@gmail.com">
        <sql>
            CREATE TABLE authorities_roles_links (
                authority_id BIGINT NOT NULL REFERENCES authorities(id),
                role_id BIGINT NOT NULL REFERENCES roles(id)
            );

--             ALTER TABLE authorities_roles_link
--                 ADD CONSTRAINT authorities_roles_links_authority_id_fkey FOREIGN KEY (authority_id) REFERENCES authorities (id);
--
--             ALTER TABLE authorities_roles_link
--                 ADD CONSTRAINT authorities_roles_links_role_id_fkey FOREIGN KEY (role_id) REFERENCES roles (id);
        </sql>
    </changeSet>

</databaseChangeLog>