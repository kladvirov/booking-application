<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.4.xsd">

    <changeSet id="12" author="wladponomar@gmail.com">
        <sql>
            CREATE TABLE IF NOT EXISTS services (
                id BIGSERIAL PRIMARY KEY,
                price DECIMAL NOT NULL CHECK (price >= 0),
                name TEXT NOT NULL CHECK(256 > LENGTH(name)),
                location TEXT NOT NULL CHECK(1024 > LENGTH(location)),
                slot TEXT NOT NULL CHECK(32 > LENGTH(slot)),
                status TEXT NOT NULL CHECK(64 > LENGTH(status)),
                created_at TIMESTAMPTZ NOT NULL,
                updated_at TIMESTAMPTZ,
                deleted_at TIMESTAMPTZ
                );

            ALTER TABLE services
                ADD COLUMN search tsvector
                    GENERATED ALWAYS AS (to_tsvector('russian', replace(services.name, 'ё', 'е'))) STORED;
            CREATE INDEX services_search_index ON services USING GIN (search);
        </sql>
    </changeSet>

</databaseChangeLog>